using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class faceToCursorY : MonoBehaviour
{
   /*  public Camera cam;

	public void Start ()
	{
		cam = GameObject.FindWithTag("Camera").GetComponent<Camera>();
	}
    void Update()
    {
		Ray ray = cam.ScreenPointToRay(Input.mousePosition);
		RaycastHit hit;
		if (Physics.Raycast(ray, out hit))
		{
			Vector3 _there = new Vector3(hit.collider.transform.position.x, transform.position.y, hit.collider.transform.position.z);
			transform.LookAt(_there);
		}
    }*/
}
